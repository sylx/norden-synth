{
  "name": "norden-synth",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && tsc -p tools && tsc -p test && vite build",
    "preview": "vite preview",
    "typecheck": "tsc && tsc -p tools && tsc -p test",
    "test": "node --test test/*.test.ts",
    "build:instruments": "node tools/sf2-extract/main.ts soundfonts/FluidR3_GM.sf2 public/instruments"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  }
}
